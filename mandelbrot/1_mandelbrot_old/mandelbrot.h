#pragma once

struct ids {
  //specs for the branches/fractals
  float xmin;
  float xmax;
  float ymin;
  float ymax;
  int iterations;
  //specs for image
  const int width;
  const int height;
  const int depth;
};

